import React, {FC} from 'react';
type ButtonProps ={
    Callback : () => void
}


const LikeButton:FC<ButtonProps>= ({Callback}) => {
    return (
        <button>

        </button>
    );
};

export default LikeButton;
